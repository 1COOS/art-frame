## 1. 设计系统更新

- [x] 1.1 更新 `AppTheme` 色彩方案：将种子色改为低饱和度灰紫色，调整深色/亮色背景色
- [x] 1.2 更新 `AppTheme` 圆角比例：卡片 28→16，按钮 18→12，Chip 16→10，输入框 20→12
- [x] 1.3 更新 `AppTheme` 字体排版：标题字间距 -0.5，正文行高 1.5
- [x] 1.4 更新 `AppTheme` 表面层级：卡片去除阴影和边框，改用半透明背景填充区分层级
- [x] 1.5 定义动效常量：在 theme 目录下新建动效常量文件，定义 quick(200ms)、standard(400ms)、emphasis(600ms) 时长和 easeOutCubic 曲线
- [x] 1.6 更新导航栏主题：底部导航高度 72→56，NavigationRail 默认仅图标模式

## 2. 导航体系改造

- [x] 2.1 移除 `AdaptiveShell` 中的 AppBar，页面标题改由各页面内容区域自行处理
- [x] 2.2 手机端底部导航栏改为紧凑样式（56px 高度，图标+微型标签）
- [x] 2.3 平板端 NavigationRail 改为仅图标模式（无文字标签），减少水平占用
- [x] 2.4 桌面端 NavigationDrawer 改为可收起的图标栏，默认收起仅显示图标

## 3. 图源页面重构

- [x] 3.1 将 SourcesPage 的 ListView 布局改为自适应网格布局（GridView），根据断点调整列数
- [x] 3.2 重构 _SourceCard 为网格项组件：大尺寸封面图为主体，底部渐变遮罩叠层显示标题和类型标签
- [x] 3.3 实现网格项的交互操作：点击主体进入播放，长按（移动端）/悬停（桌面端）显示更多操作菜单
- [x] 3.4 将顶部导入区域改为 FAB 或网格内的添加占位卡片
- [x] 3.5 实现选中图源的视觉高亮（边框高亮或角标）
- [x] 3.6 设计并实现空状态：仅有内置图源时显示风格化的「添加图源」引导卡片

## 4. 播放页面精致化

- [x] 4.1 实现控件自动隐藏机制：3秒无操作淡出，触摸/鼠标移动淡入，首次进入延长至5秒
- [x] 4.2 将控件背景改为毛玻璃效果（BackdropFilter + 半透明填充）
- [x] 4.3 简化导航按钮样式：去除按钮边框，改为纯图标 + 毛玻璃背景圆形按钮
- [x] 4.4 升级图片切换动效：从 180ms crossfade 改为 600ms 带缩放的交叉淡入淡出
- [x] 4.5 添加水平滑动手势支持：左滑下一张，右滑上一张，滑动时重置自动播放计时器
- [x] 4.6 优化空状态页面样式，与新设计系统保持一致

## 5. 设置页面适配

- [x] 5.1 移除设置页面对 AppBar 标题的依赖，在页面内容顶部添加标题区域
- [x] 5.2 更新设置卡片样式以匹配新设计系统（圆角、背景、间距）

## 6. 验证与调优

- [x] 6.1 在手机、平板、桌面三种断点下验证图源页面网格布局的响应式表现
- [x] 6.2 验证播放页面控件自动隐藏在触摸和鼠标两种输入模式下的行为
- [x] 6.3 验证深色和亮色模式下所有页面的视觉一致性
- [x] 6.4 验证图源网格叠层文字在浅色和深色封面图上的可读性
